# GENERATED FROM: templates/pod.yaml
apiVersion: v1
kind: Pod
metadata:
  name: logstash
spec:
  containers:
  - name: logstash
    image: localhost:5000/logstash:21
    command:
      - sleep
      - "3600"
    imagePullPolicy: IfNotPresent
  restartPolicy: Always